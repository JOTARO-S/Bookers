<h1>Books</h1>

  <h3>Title</h3>
    <p><%= @books.title %></p>

  <h3>Body</h3>
    <p><%= @books.body %></p>
  
<div class ="books-wrap">
<% @books.each do |list| %>
  <div class="books-box">
    <div class="books-comment">
      <span class="books-body"><%= list.body %></span>
      <span class="books-style"><%= link_to list.title, list_path(list.id) %></span>
      <%= link_to "Show", edit_books_path(@books.id) %>
      <%= link_to "Edit", edit_books_path(@books.id) %>
      <%= link_to "Destroy", books_path(@lbooks.id), method: :delete, "data-confirm" => "Are you sure?" %>
    </div>
  </div>
<% end %>
</div>

<h2>New book</h2>
<% if @books.errors.any? %>
  <%= @books.errors.count %> errors prohibited this book from being saved:
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>